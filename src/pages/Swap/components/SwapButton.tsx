import Button from '../../../shared/components/Button/components/Button'
import styles from './SwapButton.module.scss'

export default function SwapButton() {
    return (
        <Button onClick={() => {}} className={styles.swapButton}>
            Swap
        </Button>
    )
}